package com.desige.webDocuments.persistent.managers;

public class OOffice {

	/*
	public static void main(String args[]) {

		com.sun.star.uno.XComponentContext xContext = null;

		try {
			String ruta = "C:/ProgramFiles/OpenOffice.org 2.3/program/classes/juh.jar";
			ruta = URLEncoder.encode(ruta,"utf-8");
			//System.out.println(ruta);
			URL base = new URL("file:///".concat(ruta));

			//System.out.println(base.toURI());
			URL[] urls = new URL[4];
			urls[0] = new URL(base, "java_uno.jar"); // TODO get rid of it here
			urls[1] = new URL(base, "juh.jar");
			urls[2] = new URL(base, "jurt.jar");
			urls[3] = new URL(base, "ridl.jar");

			UnoClassLoader a;
			//xContext = a.newInstance(urls);
			// get the remote office component context
			xContext = com.sun.star.comp.helper.Bootstrap.bootstrap();
			if (xContext != null) {
				//System.out.println("Connected to a running office ...");
			}
		} catch (Exception e) {
			e.printStackTrace(System.err);
			System.exit(1);
		}
		System.exit(0);
	}
	*/
	
}
